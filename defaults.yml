device: eos             # Change to your preferred network device
provider: clab          # Change to virtualbox or libvirt if needed

groups:
  external:
    device: frr         # Change to your preferred external switch
  hosts:
    device: linux

# Leave the rest of this file intact
paths.plugin: [ "topology:.", "topology:../../plugin", "package:extra" ]
